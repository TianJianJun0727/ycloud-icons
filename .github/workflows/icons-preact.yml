name: YCloud Icons Preact Checks

on:
  # Automatic checks are handled by icons-frameworks.yml
  # This workflow can be triggered manually for targeted testing
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  icons-preact:
    name: YCloud Icons Preact
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm --filter @ycloud-web/icons-preact test
