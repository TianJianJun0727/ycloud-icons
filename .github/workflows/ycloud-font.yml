name: YCloud font checks

on:
  push:
    branches:
      - next
    paths:
      - .github/actions/setup-nub/**
      - packages/icons-static/package.json
      - .github/workflows/ycloud-font.yml
      - tools/build-font/**
  pull_request:
    paths:
      - .github/actions/setup-nub/**
      - .github/workflows/ycloud-font.yml
      - packages/icons-static/package.json
      - tools/build-font/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ycloud-font:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-nub
      - name: Install dependencies
        run: nub install --frozen-lockfile

      - name: Execute unit tests for build-font
        run: nub run test:font

      - name: Create font in packages/icons-static/font
        run: nub run build:font

      - name: 'Upload to Artifacts'
        uses: actions/upload-artifact@v7
        with:
          name: ycloud-font
          path: packages/icons-static/font
          retention-days: 3
